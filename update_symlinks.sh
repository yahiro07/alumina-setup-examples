#if you want to debug alumina itself, pull alumina repository in the parent directory and add symbolic link to it.
#otherwise you don't need symlink and just run yarn to install dependencies.
ln -snF ../../alumina ./packages/alumina
